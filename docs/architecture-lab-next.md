# Architecture Lab Next Increment

Decision rules are now first-class inspectable artifacts.

Next: build a scenario simulator that runs structured hypothetical inputs through the ordered rule set and exposes each fired rule, resulting state, and boundary. It must remain a product-design simulator, not a clinical recommendation engine.
