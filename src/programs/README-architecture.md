The program layer is the controlled bridge between decision matching and formulation.

It defines intent, eligibility, lifecycle and required downstream artifacts. It does not define clinical treatment, ingredient stacks or purchasing behavior.
