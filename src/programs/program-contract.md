# Program contract

A Careflux Student program is a versioned product-design object between student context matching and downstream formulation/fulfillment.

Required relationships:

Student context → program eligibility → approved program → approved formulation → daily pack specification → education plan.

No downstream artifact is implied merely because a program exists.
